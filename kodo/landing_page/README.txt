Kodo landing page
=================

How to use.

Install npm + sass

sudo apt-get install npm && npm install -g sass
￼
Generate the css using sass. From the root of this repository run the following
command:

    sass --watch landing_page/assets/sass/:landing_page/assets/css

Start a webserver I'd recommend the Live Server plugin for vscode.
