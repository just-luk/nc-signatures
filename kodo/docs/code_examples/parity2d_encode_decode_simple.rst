Parity 2D Encode and Decode
===========================

In this example shows how to use the API of ``kodo::block`` to encode and decode with
using the Parity 2D coefficient generator.

The example will walk through the basic functionality of
:ref:`kodo::block::encoder`, :ref:`kodo::block::decoder`, and
:ref:`kodo::block::generator::parity_2d`.

.. literalinclude:: ../../examples/parity_2d/encode_decode_simple.cpp
    :language: c++
    :linenos:
    :lines: 20-
